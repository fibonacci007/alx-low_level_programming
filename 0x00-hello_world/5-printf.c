#include <stdio.h>
/**
 * main -using prinf keyword to print
 *
 * Return: always returns (0)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
